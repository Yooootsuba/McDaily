from setuptools import setup, find_packages

setup(
      name="McDaily",
      version="1.0.0",
      description="API wrapper for interacting with the Taiwan McDaily app",
      author="Yooootsuba",
      license="MIT",
      packages= find_packages(),
      )
